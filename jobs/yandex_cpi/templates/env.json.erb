<%=
	params = {
		"blobstore" => {
              "options" => {
                  "blobstore_path" => p('blobstore.path')
              },
	      "provider" => "local"
	  },
	  "ntp" => p('ntp'),
	  "mbus" => p('agent.mbus'),
	}
	
        JSON.dump(params)
%>
